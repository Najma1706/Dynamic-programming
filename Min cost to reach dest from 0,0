Problem Statement

﻿You are given a 2D grid of size R x C where each cell contains a cost to enter. Your task is to calculate the minimum cost to reach the bottom-right cell (R-1, C-1) from the top-left cell (0, 0). You can only move right or down from a cell.

Input format :
First line: Two integers R and C (number of rows and columns)

Next R lines: Each contains C integers — the cost matrix

Output format :
A single integer: Minimum cost to reach cell (R-1, C-1)

Code constraints :
1 ≤ R, C ≤ 1000

0 ≤ Cost[i][j] ≤ 10⁴

Sample test cases :
Input 1 :
3 3
1 2 3
4 8 2
1 5 3
Output 1 :
11
Input 2 :
2 2
1 2
4 3


Output 2 :
6

// You are using GCC
#include <bits/stdc++.h>
using namespace std;

int main() {
    int R, C;
    cin >> R >> C;

    vector<vector<int>> cost(R, vector<int>(C));
    for (int i = 0; i < R; i++)
        for (int j = 0; j < C; j++)
            cin >> cost[i][j];

    // DP table
    vector<vector<int>> dp(R, vector<int>(C, 0));

    dp[0][0] = cost[0][0];

    // First row
    for (int j = 1; j < C; j++)
        dp[0][j] = dp[0][j - 1] + cost[0][j];

    // First column
    for (int i = 1; i < R; i++)
        dp[i][0] = dp[i - 1][0] + cost[i][0];

    // Fill rest of table
    for (int i = 1; i < R; i++) {
        for (int j = 1; j < C; j++) {
            dp[i][j] = cost[i][j] + min(dp[i - 1][j], dp[i][j - 1]);
        }
    }

    cout << dp[R - 1][C - 1];
    return 0;
}
